#include

void execute()
{
    // digitalWrite(13, HIGH);
    // delay(500);
    // digitalWrite(13, LOW);
    // delay(500);
    // Serial.write("LED cycle\n");
    int a = 0;
    int b = a + 1;
    (void)b;
}

int main()
{
    // put your main code here, to run repeatedly:
    execute();
}